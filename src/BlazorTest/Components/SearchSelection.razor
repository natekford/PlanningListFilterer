@typeparam T

<MudSelect Label="@(Label + " Select")" Variant="Variant.Outlined"
		   MultiSelection="true" Clearable="true"
		   T="T" SelectedValues="@Selection.Values"
		   SelectedValuesChanged="@Selection.SetValues">
	<Virtualize Items="@Selection.Options">
		<MudSelectItem Value="@context">@context</MudSelectItem>
	</Virtualize>
</MudSelect>

@code {
	[Parameter]
	public string Label { get; set; } = null!;
	[Parameter]
	public AnilistSearchValues<T> Selection { get; set; } = null!;
}